# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package update

on:
  schedule:
    - cron: "29 */6 * * *"
  workflow_dispatch:
    inputs:
      lanes:
        description: "Which runner lane(s) to verify on. Velnor is the sole writer in both mode."
        type: choice
        options: [velnor, github, both]
        default: velnor

permissions:
  attestations: read
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.repository }}
  cancel-in-progress: false

jobs:
  jackin_project:
    if: ${{ github.repository_owner == 'jackin-project' }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(((github.event_name == 'workflow_dispatch' && inputs.lanes == 'both') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'both')) && '[{"lane":"velnor","writer":true},{"lane":"github","writer":false}]' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'github')) && '[{"lane":"github","writer":true}]' || '[{"lane":"velnor","writer":true}]') }}
        channel: [stable]
    uses: jackin-project/velnor-actions/.github/workflows/package-updater.yml@052224913aa7c1b6750b8ed34131ae590dd961b1 # 2026.8.28
    with:
      consumer-repository: ${{ github.repository }}
      channel: ${{ matrix.channel }}
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: package-update/verified/${{ matrix.channel }}
      lane: ${{ matrix.config.lane }}
      writer: ${{ matrix.config.writer }}
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  tailrocks:
    if: ${{ github.repository_owner == 'tailrocks' }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(((github.event_name == 'workflow_dispatch' && inputs.lanes == 'both') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'both')) && '[{"lane":"velnor","writer":true},{"lane":"github","writer":false}]' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'github')) && '[{"lane":"github","writer":true}]' || '[{"lane":"velnor","writer":true}]') }}
        channel: [stable]
    uses: tailrocks/velnor-actions/.github/workflows/package-updater.yml@43fc50c6940beac5a14de96f7e7d7ef3fcac568f # 2026.8.28
    with:
      consumer-repository: ${{ github.repository }}
      channel: ${{ matrix.channel }}
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: package-update/verified/${{ matrix.channel }}
      lane: ${{ matrix.config.lane }}
      writer: ${{ matrix.config.writer }}
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  chainargos:
    if: ${{ github.repository_owner == 'ChainArgos' }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(((github.event_name == 'workflow_dispatch' && inputs.lanes == 'both') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'both')) && '[{"lane":"velnor","writer":true},{"lane":"github","writer":false}]' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'github')) && '[{"lane":"github","writer":true}]' || '[{"lane":"velnor","writer":true}]') }}
        channel: [stable]
    uses: ChainArgos/velnor-actions/.github/workflows/package-updater.yml@034bb3dfca10f405c1f6b89707aa7bc616b7d4db # 2026.8.28
    with:
      consumer-repository: ${{ github.repository }}
      channel: ${{ matrix.channel }}
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: package-update/verified/${{ matrix.channel }}
      lane: ${{ matrix.config.lane }}
      writer: ${{ matrix.config.writer }}
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  package-update-required:
    if: ${{ always() }}
    needs: [jackin_project, tailrocks, chainargos]
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || (github.event_name != 'workflow_dispatch' && vars.VELNOR_AUTOMATIC_LANES == 'github')) && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 5
    steps:
      - env:
          OWNER: ${{ github.repository_owner }}
          JACKIN: ${{ needs.jackin_project.result }}
          TAILROCKS: ${{ needs.tailrocks.result }}
          CHAINARGOS: ${{ needs.chainargos.result }}
        run: |
          set -euo pipefail
          case "$OWNER" in
            jackin-project) test "$JACKIN" = success; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = skipped ;;
            tailrocks) test "$JACKIN" = skipped; test "$TAILROCKS" = success; test "$CHAINARGOS" = skipped ;;
            ChainArgos) test "$JACKIN" = skipped; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = success ;;
            *) echo "unrecognized owner" >&2; exit 1 ;;
          esac
