# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package update

on:
  schedule:
    - cron: "29 */6 * * *"
  workflow_dispatch:

permissions:
  attestations: read
  contents: read

jobs:
  jackin_project:
    if: ${{ github.repository_owner == 'jackin-project' }}
    uses: jackin-project/velnor-actions/.github/workflows/package-updater.yml@6b854dccd06b9202e574c664e6fb2d98cb3aaf13 # 2026.8.24
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  tailrocks:
    if: ${{ github.repository_owner == 'tailrocks' }}
    uses: tailrocks/velnor-actions/.github/workflows/package-updater.yml@5dc0863e32b72e0cd18a3551ec80db3c3ecc19f0 # 2026.8.24
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  chainargos:
    if: ${{ github.repository_owner == 'ChainArgos' }}
    uses: ChainArgos/velnor-actions/.github/workflows/package-updater.yml@1cd9ec6f0f059d572d143b46145d911dc35b8cef # 2026.8.24
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "643c5341b160be151a7fae19b89b6a4f8ab3b275"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  package-update-required:
    if: ${{ always() }}
    needs: [jackin_project, tailrocks, chainargos]
    runs-on: ${{ 'ubuntu-26.04' }}
    steps:
      - env:
          OWNER: ${{ github.repository_owner }}
          JACKIN: ${{ needs.jackin_project.result }}
          TAILROCKS: ${{ needs.tailrocks.result }}
          CHAINARGOS: ${{ needs.chainargos.result }}
        run: |
          set -euo pipefail
          case "$OWNER" in
            jackin-project) test "$JACKIN" = success; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = skipped ;;
            tailrocks) test "$JACKIN" = skipped; test "$TAILROCKS" = success; test "$CHAINARGOS" = skipped ;;
            ChainArgos) test "$JACKIN" = skipped; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = success ;;
            *) echo "unrecognized owner" >&2; exit 1 ;;
          esac
